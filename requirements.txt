# AegisRL™ – Project-wide Python dependencies
# Use: pip install -r requirements.txt
# For backend-only: pip install -r backend/requirements.txt

# Core
numpy>=1.24.0,<3
pandas>=2.0.0
pyyaml>=6.0

# Market data & features (scripts/download_data, scripts/build_features)
yfinance>=0.2.0
ta>=0.11.0

# ML & RL (scripts/train_agent, scripts/evaluate_agent, backend)
torch>=2.0.0
gymnasium>=0.29.0
stable-baselines3>=2.0.0

# API (backend; also install backend/requirements.txt for backend-only)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Optional: experiment tracking, dashboard, viz
scikit-learn>=1.3.0
mlflow>=2.0.0
streamlit>=1.28.0
plotly>=5.18.0
